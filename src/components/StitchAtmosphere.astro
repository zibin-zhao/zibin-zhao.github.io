<div class="stitch-atmosphere" aria-hidden="true">
  <i class="drift-line guide-left animate-parallax-slow"></i>
  <i class="drift-line guide-right animate-parallax-fast"></i>
  <i class="atmosphere-blob blob blob-left"></i>
  <i class="atmosphere-blob blob blob-right"></i>
  <span class="atmosphere-note note-formula animate-float">f(x) = ∫(CRISPR + AI)dx</span>
  <span class="atmosphere-note note-pathway animate-float">→ Optimizing pathway...</span>
  <span class="atmosphere-note note-question animate-float">?!!</span>
</div>

<style>
  .stitch-atmosphere {
    position: fixed;
    z-index: 0;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .stitch-atmosphere > * { position: absolute; pointer-events: none; }

  .drift-line {
    top: -10%;
    width: 2px;
    height: 250vh;
    background-image: linear-gradient(to bottom, var(--ink) 50%, transparent 50%);
    background-size: 2px 20px;
    opacity: .6;
  }

  .guide-left { left: 15%; }
  .guide-right { right: 24%; top: 5%; }

  .atmosphere-blob { display: block; border: var(--stroke); }
  .blob-left {
    top: 40%;
    left: -100px;
    width: 300px;
    height: 300px;
    background: color-mix(in srgb, var(--green-bright) 30%, transparent);
    opacity: .3;
  }
  .blob-right {
    right: -50px;
    bottom: 20%;
    width: 400px;
    height: 400px;
    border-style: dashed;
    background: color-mix(in srgb, var(--orange) 25%, transparent);
    opacity: .25;
  }

  .atmosphere-note {
    padding: 4px 8px;
    border: var(--stroke);
    background: var(--surface);
    color: var(--outline);
    font: 500 12px/1.3 var(--font-note);
    opacity: .7;
  }
  .note-formula { top: 15%; left: 60%; border-style: dashed; }
  .note-pathway { top: 45%; left: 8%; }
  .note-question {
    right: 12%;
    top: 75%;
    padding: 0;
    border: 0;
    color: var(--ink);
    font: 400 24px/1 var(--font-display);
    opacity: .8;
  }

  @media (max-width: 520px) {
    .note-formula { left: 45%; }
    .note-pathway { left: 3%; }
    .blob-right { right: -220px; }
  }
</style>
